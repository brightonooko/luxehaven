// EmailJS configuration
// Fill in your EmailJS Public Key, Service ID, and Template ID
// Get these from your EmailJS dashboard after creating a service and template.
// Do NOT commit real secrets if your repo is public; Public Key is safe to expose.

window.EMAILJS_CONFIG = {
  publicKey: "YOUR_PUBLIC_KEY",
  serviceId: "YOUR_SERVICE_ID",
  templateId: "YOUR_TEMPLATE_ID"
};
